
services:
  web:
    build:
      context: .
      dockerfile: Dockerfile

    image: chinmmayi-flask-app:1.0

    container_name: chinmmayi-flask-web

    ports:
      - "5000:5000"

    environment:
      PYTHONUNBUFFERED: "1"

    restart: unless-stopped